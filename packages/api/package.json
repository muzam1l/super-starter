{
  "name": "@{workspace}/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "dev": "bun db:studio",
    "db:studio": "drizzle-kit studio",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "auth:schema:generate": "npx @better-auth/cli generate --output ./src/schema/auth.ts",
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.9.2",
    "@trpc/client": "^11.4.4",
    "@trpc/react-query": "^11.4.4",
    "@trpc/server": "^11.4.4",
    "@{workspace}/utils": "workspace:*",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  }
}